from django import forms
#from .models import Farmers

#class RegisterForm(forms.ModelForm):

    #class Meta:

        #model = Farmers
        #fields = '__all__'

  """
    first_name = forms.CharField(max_length=20)

    middle_name = forms.CharField(max_length=20, required=False)

    last_name = forms.CharField(max_length=25)

    User_name = forms.CharField(label = 'Username', max_length=25, required=True)

    Email = forms.EmailField(label = 'Email',required=True)

    Password = forms.CharField(widget=forms.PasswordInput, label='Password')

    Message = forms.CharField(widget=forms.Textarea,label='Description', required=False)
    """

    